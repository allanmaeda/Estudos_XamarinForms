<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XF_ListViewAdv4.MainPage">

	<StackLayout>
	    <ListView x:Name="ListView" RowHeight="100">
	        <ListView.ItemTemplate>
	            <DataTemplate>
	                <ViewCell>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Label Text="{Binding Texto1}" Grid.Row="0" Grid.Column="0"/>
                            <Button Text="{Binding Texto2}" Grid.Row="0" Grid.Column="1"/>
                            <Label Text="{Binding Texto3}" Grid.Row="1" Grid.Column="0"/>
                            <Button Text="{Binding Texto4}" Grid.Row="1" Grid.Column="1"/>
                        </Grid>
                    </ViewCell>
	            </DataTemplate>
	        </ListView.ItemTemplate>
	    </ListView>
    </StackLayout>

</ContentPage>